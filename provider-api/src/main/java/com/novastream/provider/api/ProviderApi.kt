package com.novastream.provider.api

/**
 * Provider API marker (v15). Full [StreamingProvider] interface remains in :app
 * until Hilt multi-module migration completes.
 */
object ProviderApi {
    const val MIN_PROVIDERS_TARGET = 100
}
