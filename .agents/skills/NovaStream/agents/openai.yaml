interface:
  display_name: "NovaStream"
  short_description: "Repo-specific patterns and workflows for NovaStream"
  default_prompt: "Use the NovaStream repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true